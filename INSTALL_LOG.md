# Project Install Log

Current state: the independent npm acquisition completed at `2026-08-28T06:02:10Z` with exact locked dependencies, verified registry signatures, official-registry-only lockfile URLs, no high-severity audit findings, and passing direct imports. The exact Godot `4.7.2-stable` app passed the complete macOS trust gate outside the Codex filesystem/process sandbox, was installed self-contained with matching `4.7.2.stable` templates under ignored `.tools/godot/4.7.2/`, and completed controlled headless runtime verification at `2026-08-28T06:44:18Z`. The earlier unauthorized headless executions remain a separate historical incident and do not count as post-clearance verification; their quarantine is retained.

## Policy

- Record every project-related editor, executable, package-manager operation, package, plug-in, add-on, template, demo, or other software acquisition in this file. Add the entry before installation and complete its verification/result fields immediately afterward.
- Use only an official publisher, official registry, or a specifically reviewed and user-approved repository. Do not use unknown mirrors, opaque install scripts, or artifacts whose publisher or provenance cannot be established.
- Pin the exact requested version. For package-manager installs, record the direct packages and versions here and retain the lockfile that identifies transitive packages and integrity hashes.
- Verify a publisher checksum, signature, notarization, registry integrity field, or equivalent when one is available. Stop on a mismatch, an unexpected publisher, malware warning, or unexplained installer behavior.
- Keep the install scoped to this project when practical. Record any system-wide effect explicitly and provide tested or publisher-documented removal steps.
- Never record credentials, access tokens, license keys, hardware identifiers, or other secrets.
- General permission for needed, trusted, logged installs does not approve an implementation approach or a specific proposed resource. Record the decision or request that authorizes the exact install.

## Entry template

### YYYY-MM-DD — Package or tool name

- Date and time (UTC):
- Status: `planned` / `completed` / `failed` / `removed`
- Exact package and version:
- Publisher and source URL:
- Checksum, signature, notarization, or registry integrity evidence:
- Exact command or installation method:
- Purpose:
- Scope and install location:
- Authorizing decision or request:
- Trust and malware checks:
- Verification and result:
- Removal steps:

### 2026-08-28 — Godot Engine standard editor and export templates

- Date and time (UTC): acquisition began `2026-08-28T05:40:04Z`; controlled installation and runtime verification completed `2026-08-28T06:44:18Z`
- Status: `completed`
- Current trust/runtime status: **`completed`**; the exact app passed the outside-sandbox trust gate, matching templates are installed self-contained, and the cleared binary passed the controlled headless checks below.
- Exact package and version: Godot Engine `4.7.2-stable`, standard/GDScript Universal macOS editor `Godot_v4.7.2-stable_macos.universal.zip`; matching standard templates `Godot_v4.7.2-stable_export_templates.tpz`; no .NET build.
- Publisher and source URL: Godot Engine official builds, [release](https://github.com/godotengine/godot-builds/releases/tag/4.7.2-stable); exact [editor asset](https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_macos.universal.zip); exact [template asset](https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_export_templates.tpz); documented macOS signer/notarization identity Prehensile Tales B.V.
- Checksum, signature, notarization, or registry integrity evidence: expected editor SHA-256 `c58a24e31d720be9d62f60cb5627c4e695fb72f21b0cfe1bc9ccaa9a3b3ba63e`; expected TPZ SHA-256 `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011`; release also publishes `SHA512-SUMS.txt`; live `codesign`, `spctl`, and `stapler` results are recorded below. No detached artifact signature was found in the approved trust review.
- Exact command or installation method: use `/usr/bin/curl --fail --location --proto '=https' --tlsv1.2` for the two exact official GitHub release URLs into the versioned project-local staging directory; verify SHA-256 before extraction; extract only the verified ZIP with `/usr/bin/ditto -x -k`; validate signature/notarization/architectures; enable documented self-contained mode with `_sc_` beside the intact app; safely extract the already-hashed TPZ under adjacent `editor_data/export_templates/4.7.2.stable/` after archive-path inspection.
- Purpose: native editor/runtime and matching private macOS export support for the approved first-playable bundle.
- Scope and install location: `/Users/philipp/cowork/treasure-island-game/.tools/godot/4.7.2/`, including adjacent `editor_data/`; no system-wide install.
- Authorizing decision or request: owner-approved APPROACH-001 in [`discovery/APPROACH_PROPOSAL.md`](discovery/APPROACH_PROPOSAL.md) and the authoritative `approved` status in [`discovery/RESOURCE_REQUESTS.md`](discovery/RESOURCE_REQUESTS.md), both recorded 2026-08-27.
- Trust and malware checks: exact hashes; strict code-signature validation; expected signer inspection; Gatekeeper/notarization assessment; stapled-ticket validation; allow macOS malware checks; stop on any mismatch or warning and never disable Gatekeeper globally.
- Initial sandboxed acquisition result: **Stopped before self-contained setup or authorized execution.** Both exact official release assets downloaded successfully. Editor ZIP: `170622178` bytes, live SHA-256 `c58a24e31d720be9d62f60cb5627c4e695fb72f21b0cfe1bc9ccaa9a3b3ba63e` (match). Template TPZ: `1281349702` bytes, live SHA-256 `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011` (match). The editor ZIP contained 153 archive paths; the pre-extraction absolute/`..` path check passed. `/usr/bin/ditto -x -k` extracted `Godot.app`, after which strict verification failed for both `x86_64` and `arm64`: `invalid signature (code or signature have been modified)`. `codesign --display --verbose=4` returned:

  ```text
  Executable=/Users/philipp/cowork/treasure-island-game/.tools/godot/4.7.2/Godot.app/Contents/MacOS/Godot
  Identifier=org.godotengine.godot
  Format=app bundle with Mach-O universal (x86_64 arm64)
  CodeDirectory v=20500 size=1313185 flags=0x10000(runtime) hashes=41026+7 location=embedded
  VersionPlatform=1
  VersionMin=720896
  VersionSDK=1704192
  Hash type=sha256 size=32
  CandidateCDHash sha256=39be419ecfc45fc922f4b7922ca8058c59daaed0
  CandidateCDHashFull sha256=39be419ecfc45fc922f4b7922ca8058c59daaed0526103668d6d6ae8cf327f53
  Hash choices=sha256
  CMSDigest=39be419ecfc45fc922f4b7922ca8058c59daaed0526103668d6d6ae8cf327f53
  CMSDigestType=2
  Executable Segment base=0
  Executable Segment limit=160464896
  Executable Segment flags=0x1
  Page size=4096
  CDHash=39be419ecfc45fc922f4b7922ca8058c59daaed0
  Signature size=9056
  Authority=(unavailable)
  Notarization Ticket=stapled
  Info.plist=not bound
  TeamIdentifier=6K46PWY5DM
  Runtime Version=26.1.0
  Sealed Resources version=2 rules=13 files=75
  Internal requirements count=1 size=184
  ```

  Gatekeeper failed with `internal error in Code Signing subsystem`. `xcrun stapler validate` exited `66`, reporting that the app “does not exist”/could not be opened and underlying `kLSDataUnavailableErr`, despite the bundle being present. `lipo -archs` returned the expected `x86_64 arm64`, but architecture success does not override the signature/notarization failures. The app was not run, so no `--version` result exists; `_sc_` and `editor_data/` were not created; the verified TPZ was not inspected further or installed; template recognition was not attempted. This is the required stop condition, not a successful installation.
- Security-first signature diagnosis (`2026-08-28T05:47:41Z`–`2026-08-28T05:53:19Z` UTC; diagnostic agent `/root/godot_signature_triage_sol`): **no clean validation; trust gate remains failed.** The diagnostic did not execute or open Godot, install templates, create self-contained state, remove quarantine, or alter the staged ZIP/TPZ or isolated app.
  - Official provenance and archive safety: GitHub's API for the official `godotengine/godot-builds` `4.7.2-stable` release reported the exact editor asset URL already recorded above, uploader `GodotBuilder`, publication `2026-08-18T15:56:46Z`, asset size `170622178`, and publisher-side digest `sha256:c58a24e31d720be9d62f60cb5627c4e695fb72f21b0cfe1bc9ccaa9a3b3ba63e`. The staged ZIP's live SHA-256 and size match those values. `unzip -tq` found no compressed-data errors. All 153 archive paths passed checks for absolute paths, `..` traversal components, and backslash separator ambiguity; the archive and extracted trees contain no symlinks.
  - Fresh extraction controls: the diagnostic extracted separate controls under a `mktemp` directory using Apple-shipped `/usr/bin/unzip` and `/usr/bin/tar` (bsdtar), without overwriting the isolated `/usr/bin/ditto` copy. Each tree contains exactly 153 filesystem objects and 80 regular files. Complete normalized path/type/mode/size inventories and SHA-256 inventories had zero differences across all three extraction methods. Extraction-induced mutation is therefore ruled out with high confidence.
  - Extended attributes: the staged ZIP has only `com.apple.provenance`. Every object in each of the three extracted trees has only that same provenance attribute; none has `com.apple.quarantine`. This observation is diagnostic only and was not used to waive any signature, Gatekeeper, or notarization requirement.
  - Apple verification results were identical on the isolated and both fresh controls. Whole-bundle `codesign --verify --deep --strict` and explicit `--arch x86_64` and `--arch arm64` verification all exited `1` with `invalid signature (code or signature have been modified)`. Both slices display identifier `org.godotengine.godot`, Team ID `6K46PWY5DM`, hardened-runtime flags, and a stapled-ticket field, but `Authority=(unavailable)`. Gatekeeper exited `1` with `internal error in Code Signing subsystem`; `stapler validate -v` exited `66` with `kLSDataUnavailableErr`. `--ignore-resources` and direct-executable checks still failed, so the result is not explained by bundle-resource sealing alone.
  - Independent embedded-signature evidence: the `arm64` CodeDirectory SHA-256 is `39be419ecfc45fc922f4b7922ca8058c59daaed0526103668d6d6ae8cf327f53`, exactly matching its CMS message digest; OpenSSL successfully verified the detached CMS signature against that CodeDirectory. The CMS contains `Developer ID Application: Prehensile Tales B.V. (6K46PWY5DM)`, signing time `2026-08-17T14:50:59Z`, and an Apple timestamp token at `2026-08-17T14:50:59Z`. An independent page-hash audit found zero mismatches across all 41,026 `arm64` code slots. Corroborating checks also found zero mismatches across all 44,066 `x86_64` code slots, verified that slice's detached CMS against CodeDirectory hash `4870ba937182a4ae337f9a39be7f6b693e17f16dfc78df3dfc69e5e1feca4811`, and confirmed that `_CodeSignature/CodeResources` hashes to the embedded special-slot value `5549b72ce8612b978550b5f764641c3db3a58167289a5731695f4b6a7a78ee79`. These facts argue against byte corruption but do not substitute for Apple's failed trust decision.
  - Host control and toolchain evidence: at `2026-08-28T05:53:19Z` the host reported macOS `26.5.2` build `25F84`, Apple silicon `arm64`, Command Line Tools `26.6.0.0.1781586589`, Gatekeeper assessments enabled, and SIP enabled. The date is consistent with the signing time and certificate validity window. Crucially, the same host's Apple-supplied `/System/Applications/Calculator.app` and `/usr/bin/true` also failed strict `codesign` verification with `CSSMERR_TP_NOT_TRUSTED`, displayed `Authority=(unavailable)`, and Calculator failed Gatekeeper with the same internal Code Signing subsystem error; a third-party VLC control failed similarly. This proves a host-wide local Apple trust/code-signing evaluation fault is present. Official GitHub issue searches in `godotengine/godot-builds` and `godot` found no `4.7.2` report matching `invalid signature`, `codesign`, or notarization at the check time.
  - Conclusion and residual uncertainty: the failure is **not extraction-induced**. The immediate blocking cause is local Apple trust/code-signing subsystem behavior, with high confidence, because Apple system code fails on the same host. The underlying local OS/trust-state cause was not isolated. Because this host cannot produce a healthy Apple verification baseline, this diagnostic cannot independently exclude a simultaneous upstream Godot packaging/certificate-chain compatibility defect; the app therefore remains untrusted. No replacement or installation is recommended from these controls.
  - Safest next action: leave the exact assets isolated and do not execute Godot or install templates. Restore a healthy Apple verification baseline through Apple-supported maintenance (confirm automatic date/time, restart, apply an applicable stable macOS update, and use Apple Support if Apple-supplied code still returns `CSSMERR_TP_NOT_TRUSTED`). Require `/usr/bin/true` and Calculator to validate normally first; then repeat fresh extraction and the complete `codesign` per-slice/whole-bundle, `spctl`, and `stapler` gates. An independent fully updated trusted Mac may be used to distinguish an upstream artifact problem without changing the approved asset/version. Any acquisition-method or version substitution remains a separate before -> after approval decision.
- Later incident, outside this diagnostic: the gameplay agent subsequently executed the still-isolated Godot binary headlessly four times before the project lead stopped it. The diagnostic agent did not perform or authorize those executions. The resulting side effects were inventoried and hashed, then moved recoverably under `.tools/quarantine/unauthorized-godot-run-2026-08-27/`. The incident review observed no GUI warning and no successful writes to the requested user/cache directories. Those observations do not rehabilitate the signature or waive the failed trust gate; the app remains stopped, isolated, and unapproved for further execution.
- Unsandboxed macOS trust-gate rerun (`2026-08-28T06:08:52Z`–`2026-08-28T06:12:26Z` UTC; verification agent `/root/godot_gate_unsandboxed_sol`): **Trust-gate status: `passed-but-not-yet-executed` under this clearance.** This result supersedes the earlier sandbox-derived trust conclusion while preserving its output as historical evidence. Every app/archive inspection was read-only and ran outside the Codex filesystem/process sandbox. This rerun did not execute or open Godot, install templates, create `_sc_` or `editor_data/`, remove attributes, alter trust policy, or change the app or staged archives. `stapler validate` contacted Apple's ticket service and wrote only its documented ephemeral validation ticket under the system temporary directory.
  - Healthy Apple controls: `/usr/bin/codesign --display --verbose=4 /System/Applications/Calculator.app` exited `0` and displayed `Authority=Software Signing`, `Authority=Apple Code Signing Certification Authority`, and `Authority=Apple Root CA`. Whole-bundle `/usr/bin/codesign --verify --deep --strict --verbose=4` exited `0` with `valid on disk` and `satisfies its Designated Requirement`; `/usr/sbin/spctl --assess --type execute --verbose=4` exited `0` with `accepted` and `source=Apple System`. `/usr/bin/true` likewise displayed the complete Apple authority chain and passed strict `codesign` verification with exit `0`. The prior Apple-control failures were therefore artifacts of the earlier sandboxed environment, not a host-wide trust failure.
  - Exact app signature: whole-bundle `/usr/bin/codesign --verify --deep --strict --verbose=4` and the same strict command with `--arch x86_64` and `--arch arm64` each exited `0`, reporting the exact `/Users/philipp/cowork/treasure-island-game/.tools/godot/4.7.2/Godot.app` as `valid on disk` and satisfying its Designated Requirement. Display for the bundle and both slices returned identifier `org.godotengine.godot`, hardened-runtime flags, `Notarization Ticket=stapled`, Team ID `6K46PWY5DM`, and the complete expected chain `Developer ID Application: Prehensile Tales B.V. (6K46PWY5DM)` -> `Developer ID Certification Authority` -> `Apple Root CA`. The `x86_64` full CodeDirectory hash remained `4870ba937182a4ae337f9a39be7f6b693e17f16dfc78df3dfc69e5e1feca4811`; the `arm64` full CodeDirectory hash remained `39be419ecfc45fc922f4b7922ca8058c59daaed0526103668d6d6ae8cf327f53`.
  - Gatekeeper and notarization: `/usr/sbin/spctl --assess --type execute --verbose=4` exited `0` with `accepted` and `source=Notarized Developer ID`. `/usr/bin/xcrun stapler validate -v` exited `0`, resolved the same arm64 CDHash `39be419ecfc45fc922f4b7922ca8058c59daaed0`, signing ID `org.godotengine.godot`, Team ID `6K46PWY5DM`, secure timestamp `2026-08-17 14:50:59 +0000`, received HTTP `200` from Apple's ticket service, and ended `The validate action worked!`.
  - Architectures and attributes: `/usr/bin/lipo -archs` returned exactly `x86_64 arm64`; `/usr/bin/file` confirmed a two-architecture Mach-O universal executable. A corrected recursive extended-attribute-name count covered all `153` bundle objects and found only `com.apple.provenance` on all `153`; `com.apple.quarantine` was absent. An initial presentation-only formatter emitted literal `\\t` text and misleading zero summary counters, so it was discarded and immediately rerun with the corrected name-only aggregation reported here; no bundle content or attribute was changed in either inspection.
  - Archive/hash recheck: the exact staged editor ZIP remained `170622178` bytes with SHA-256 `c58a24e31d720be9d62f60cb5627c4e695fb72f21b0cfe1bc9ccaa9a3b3ba63e`; the exact staged template TPZ remained `1281349702` bytes with SHA-256 `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011`. Both match the approved publisher values. Non-extracting `/usr/bin/unzip -tq` checks exited `0` for both archives with `No errors detected in compressed data`.
  - Conclusion: with healthy Apple controls and unchanged Godot hashes, code directories, identifier, Team ID, signer, and ticket, every prior Apple-tool failure is reproduced only inside the earlier Codex sandbox and passes outside it. No signature, notarization, hash, architecture, publisher, or archive-integrity mismatch remains. The trust gate is passed, but this is deliberately not runtime evidence: Godot was not launched, `--version` was not invoked, self-contained mode remains uncreated, and templates remain uninstalled.
- Approved self-contained completion and controlled verification (`2026-08-28T06:20:54Z`–`2026-08-28T06:44:18Z` UTC; installation agent `/root/godot_install_finish_sol`): **completed.** The final archive SHA-256 values remained exactly `c58a24e31d720be9d62f60cb5627c4e695fb72f21b0cfe1bc9ccaa9a3b3ba63e` for the editor ZIP and `f298490b8d44d934be425a5a65a51bf15f422428b229a06a6e11d9ffea248011` for the TPZ. TPZ inspection found exactly `35` regular-file paths under the sole `templates/` root, no absolute/traversal/backslash path, duplicate, or symlink entry, and exact `version.txt` content `4.7.2.stable`. An empty `_sc_` sentinel was created beside and not inside `Godot.app`. The verified TPZ was extracted once into a project-local temporary directory, validated, and atomically moved to `.tools/godot/4.7.2/editor_data/export_templates/4.7.2.stable/`; its installed `35`-file inventory exactly matches the archive and its retained SHA-256 inventory was unchanged at final verification.
  - App integrity remained intact after the adjacent self-contained/template setup and after runtime checks: strict deep `codesign` exited `0` with `valid on disk` and `satisfies its Designated Requirement`; Gatekeeper exited `0` with `accepted` / `source=Notarized Developer ID`; stapler validation exited `0` and reported `The validate action worked!`; final architectures were `x86_64 arm64`. No quarantine removal, trust-policy change, GUI launch, export, system/global installation, or extra download occurred.
  - The cleared executable returned `4.7.2.stable.official.ed1daf0bf` with exit `0`, matching the approved `4.7.2-stable` version and release commit prefix. A fresh self-contained `--headless --editor --path . --quit` import exited `0` with no warnings. `headless_scene_parse.gd` exited `0` with `PASS: parsed 4 gameplay scenes`; `headless_gameplay_contract.gd` exited `0` with `PASS: gameplay inputs, boundary cases, scene hierarchies, and layer defaults`; and `validate_generated_world.gd` exited `0` with `PASS: 735 playable + 4 context rows across 38 chunks; manifest 5899ebe131bbf05525fedebdac703a321a7aaf95523a57960cb937085068cfaa`. Independent `node tools/validate_godot_world.mjs generated/world` exited `0` with `739` source rows, `38` chunks, `701` physical objects, and the same content hash.
  - Stop conditions were honored during iteration. A GDScript string-NUL bug, JSON-number type mismatch, triangle-tolerance mismatch, and one approved indirect-composite validation mismatch were corrected by the runtime owner before clean reruns. A generated coverage CSV auto-import was stopped, inventoried, hashed, and moved recoverably with its `.godot` cache under `.tools/quarantine/post-clearance-generated-csv-import-2026-08-28/`; `generated/.gdignore` now supplies the approved scan boundary while the runtime test proves direct `FileAccess`. The earlier unauthorized-execution quarantine remains separate and untouched.
  - Final side-effect checks found zero `.csv.import` or `.translation` files under either `data/` or `generated/`; the frozen raw source CSV still hashes to `fdf57f37f5507a7cb9b64a7fc530eabb22e29f3742324d5347c0f6b1949cb4cb`; the generator-owned world is the canonical `44` files with manifest SHA-256 `82ec5d835965c7f9df0343e92c739c4bc2a6b98190034a2e0c22532e6bf7eb1e`. Exact command output, exit codes, archive/template inventories, hashes, side-effect inventories, and preserved pre-fix logs are retained under ignored `.tools/godot/4.7.2/logs/install-verification/`; the retained logs contain no personal identifiers.
- Removal steps: close Godot and move only `/Users/philipp/cowork/treasure-island-game/.tools/godot/4.7.2/` to Trash; separately review disposable project `.godot/` cache; update status to `removed`.

### 2026-08-28 — polygon-clipping npm package

- Date and time (UTC): `2026-08-28T05:58:06Z`
- Status: `completed`
- Exact package and version: `polygon-clipping@0.15.7` as a development dependency.
- Publisher and source URL: Mike Fogel / npm user `mfogel`; [npm version](https://www.npmjs.com/package/polygon-clipping/v/0.15.7); [registry metadata](https://registry.npmjs.org/polygon-clipping/0.15.7); [upstream tag](https://github.com/mfogel/polygon-clipping/releases/tag/v0.15.7).
- Checksum, signature, notarization, or registry integrity evidence: expected npm SRI `sha512-nhfdr83ECBg6xtqOAJab1tbksbBAOMUltN60bU+llHVOL0e5Onm1WpAXXWXVB39L8AJFssoIhEVuy/S90MmotA==`; SHA-1 registry shasum `3823ca1e372566f350795ce9dd9a7b19e97bdaad`; registry ECDSA signature present in reviewed metadata; npm `gitHead` matches upstream `25f2a460ca7bd750d958d3d83b0037a32fcf75f6`; upstream commit signature verified but key now expired; no npm provenance attestation found.
- Exact command or installation method: combined exact project-local `npm install --save-dev --save-exact --ignore-scripts --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache polygon-clipping@0.15.7 earcut@3.2.3`, followed by `npm audit signatures`, lockfile inspection, `npm ls`, and `npm audit --audit-level=high` against the same official registry/cache.
- Purpose: deterministic polygon Boolean operations in the approved offline OSM conversion step.
- Scope and install location: project `devDependencies`, `node_modules/`, `package-lock.json`, and `.tools/npm-cache/`; no global package install.
- Authorizing decision or request: owner-approved APPROACH-002 in [`discovery/APPROACH_PROPOSAL.md`](discovery/APPROACH_PROPOSAL.md) and the authoritative `approved` status in [`discovery/RESOURCE_REQUESTS.md`](discovery/RESOURCE_REQUESTS.md), both recorded 2026-08-27.
- Trust and malware checks: SRI enforced by npm/lockfile; registry signatures must verify; lifecycle scripts suppressed; registry hosts and full dependency tree reviewed; `npm audit` result recorded. Stop on missing/invalid signatures, unexpected hosts, lifecycle execution, or a high-severity audit issue.
- Verification and result: **Completed independently of the failed Godot acquisition.** Node `v26.7.0` and npm `11.19.0` ran the exact combined command. An initial sandboxed invocation had no registry access/output and was interrupted without acquiring packages; the same exact command then ran with approved network access and reported `added 4 packages, and audited 5 packages` and `found 0 vulnerabilities`. The manifest retains `private: true`, `type: module`, and exact development dependencies. `package-lock.json` lockfile version 3 contains only `https://registry.npmjs.org/` tarballs and these exact version/SRI pairs: direct `polygon-clipping@0.15.7` / `sha512-nhfdr83ECBg6xtqOAJab1tbksbBAOMUltN60bU+llHVOL0e5Onm1WpAXXWXVB39L8AJFssoIhEVuy/S90MmotA==`; direct `earcut@3.2.3` / `sha512-vnS4AVwp1KHAF13i1vp1/2D5evWy3k5u/iW/B81QVsUZtV8cv2tU0b2VNFlqvh4kYwrFMDdjPCfAmfyJW9y14Q==`; transitive `robust-predicates@3.0.3` / `sha512-NS3levdsRIUOmiJ8FZWCP7LG3QpJyrs/TE0Zpf1yvZu8cAJJ6QMW92H1c7kWpdIHo8RvmLxN/o2JXTKHp74lUA==`; transitive `splaytree@3.2.3` / `sha512-7OXrNWzy6CK+r7Ch9OLPBDTKfB6XlWHjX4P0RU5B3IgFuWPeYN0XtRtlexGRjgbQxpfaUve6jTAwBGWuGntz/w==`. A local validator confirmed every non-root lock entry has a version, SHA-512 integrity, HTTPS scheme, and exact `registry.npmjs.org` host. `npm audit signatures --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache` exited 0: four packages had verified registry signatures; none was missing or invalid; one verified attestation belongs to transitive `splaytree@3.2.3`, while no attestation is claimed for either direct dependency. `npm ls --all` resolved `polygon-clipping@0.15.7` to `robust-predicates@3.0.3` and `splaytree@3.2.3`, alongside direct `earcut@3.2.3`. The first sandboxed vulnerability-audit query failed only with `ENOTFOUND registry.npmjs.org`; the identical approved-network `npm audit --audit-level=high --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache` exited 0 with `found 0 vulnerabilities`. npm's debug argv records `--ignore-scripts`, and no lifecycle command or lifecycle output occurred. A script-free ESM smoke command imported both packages, triangulated a square to six indices with `earcut`, and intersected two squares to the expected unit-square polygon with `polygon-clipping`. `package.json` and `package-lock.json` are retained; `.tools/` and `node_modules/` remain ignored.
- Removal steps: `npm uninstall --save-dev --ignore-scripts polygon-clipping earcut`; review manifest/lockfile diff; remove only unused transitive content and move the project-local npm cache to Trash if unused; update status to `removed`.

### 2026-08-28 — USGS 3DEP TI+YBI terrain crop

- Date and time: 2026-08-28 local project work session
- Status: `completed`
- Publisher/source: U.S. Geological Survey 3D Elevation Program ImageServer; exact locked raster `OBJECTID=71423`, `USGS 1 Meter 10 x55y419 CA_SanFrancisco_B23`; 2023-04-20 acquisition date; NAVD 88
- Exact method: account-free official `exportImage` request, WGS84 bbox `[-122.3800,37.8060,-122.3580,37.8330]`, `1669x2048`, F32 TIFF, bilinear interpolation, locked-raster mosaic rule
- Scope: project source only at `data/terrain/treasure-island-ybi-usgs-3dep-2023.tif`; no software, service, account, credential, or system-wide installation
- Result: `10,160,678` bytes, SHA-256 `3af54acdd116f3e253af8ece5ec501a08dff94cfca47b7f05f2c326c9ba33f3f`; direct float-tile validation found `2,492,288` valid samples spanning `-1.080` to `104.756 m` and all five pinned island samples matched
- Rejected attempt: an initial `2048x3072` request exceeded the service's advertised `maxDownloadSizeLimit=2048`; its malformed sparse metadata was detected, the file was rejected, and the accepted within-limit crop replaced it
- Purpose: reproducible bare-earth vertical geometry for playable Treasure Island and non-playable YBI while the frozen OSM snapshot remains authoritative for all horizontal feature geometry
- Full provenance and reproduction detail: `data/terrain/README.md`
- Removal: remove only `data/terrain/treasure-island-ybi-usgs-3dep-2023.tif` if terrain generation is intentionally being retired; generated world artifacts must then be regenerated and will fail closed while the source contract still requires it

### 2026-08-28 — earcut npm package

- Date and time (UTC): `2026-08-28T05:58:06Z`
- Status: `completed`
- Exact package and version: `earcut@3.2.3` as a development dependency.
- Publisher and source URL: Vladimir Agafonkin / npm user `mourner`, maintained in the Mapbox repository; [npm version](https://www.npmjs.com/package/earcut/v/3.2.3); [registry metadata](https://registry.npmjs.org/earcut/3.2.3); [upstream release](https://github.com/mapbox/earcut/releases/tag/v3.2.3).
- Checksum, signature, notarization, or registry integrity evidence: expected npm SRI `sha512-vnS4AVwp1KHAF13i1vp1/2D5evWy3k5u/iW/B81QVsUZtV8cv2tU0b2VNFlqvh4kYwrFMDdjPCfAmfyJW9y14Q==`; SHA-1 registry shasum `74aec19555a7e28773429826729d2e4bfeb19022`; registry ECDSA signature present in reviewed metadata; npm `gitHead` matches upstream `f183d7a06535290bc22163ca5ca2248e4d5a04c5`; upstream tag/commit unsigned and no npm provenance attestation found.
- Exact command or installation method: combined exact project-local `npm install --save-dev --save-exact --ignore-scripts --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache polygon-clipping@0.15.7 earcut@3.2.3`, followed by `npm audit signatures --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache`, full lockfile inspection, `npm ls --all`, `npm audit --audit-level=high --registry=https://registry.npmjs.org/ --cache=.tools/npm-cache`, and direct ESM smoke imports without package scripts.
- Purpose: triangulate validated polygons and holes in the approved offline OSM conversion step.
- Scope and install location: project `devDependencies`, `node_modules/`, `package-lock.json`, and `.tools/npm-cache/`; no global package install.
- Authorizing decision or request: owner-approved APPROACH-002 in [`discovery/APPROACH_PROPOSAL.md`](discovery/APPROACH_PROPOSAL.md) and the authoritative `approved` status in [`discovery/RESOURCE_REQUESTS.md`](discovery/RESOURCE_REQUESTS.md), both recorded 2026-08-27.
- Trust and malware checks: SRI enforced by npm/lockfile; registry signatures must verify; lifecycle scripts suppressed; registry hosts and full dependency tree reviewed; unsigned upstream/no-provenance limitation explicitly retained; `npm audit` result recorded.
- Verification and result: **Completed in the same exact combined acquisition described above.** `earcut@3.2.3` is an exact development dependency, and its lock entry resolves only to `https://registry.npmjs.org/earcut/-/earcut-3.2.3.tgz` with the expected `sha512-vnS4AVwp1KHAF13i1vp1/2D5evWy3k5u/iW/B81QVsUZtV8cv2tU0b2VNFlqvh4kYwrFMDdjPCfAmfyJW9y14Q==` integrity. The full four-package lock tree uses only the official registry and has complete SHA-512 integrities; `npm audit signatures` exited 0 with four verified registry signatures, zero missing/invalid signatures, and one attestation only for transitive `splaytree@3.2.3`; `npm ls --all` resolved the expected direct and transitive versions; the approved-network high-severity audit exited 0 with `found 0 vulnerabilities`; lifecycle scripts were suppressed and none ran. A script-free ESM import triangulated a square to six indices. The exact command, sandbox-only retry evidence, full dependency/SRI inventory, and `polygon-clipping` smoke result are recorded in the companion entry above. `package.json` and `package-lock.json` are retained; `.tools/` and `node_modules/` remain ignored.
- Removal steps: remove through the combined `npm uninstall --save-dev --ignore-scripts polygon-clipping earcut`; review manifest/lockfile diff; remove only unused transitive content and move the project-local npm cache to Trash if unused; update status to `removed`.

### 2026-08-28 — Kenney Nature Kit vegetation models

- Date and time (UTC): `2026-08-28T22:17:11Z`
- Status: `completed`
- Exact package and version: Kenney `Nature Kit`; embedded license identifies version `2.1`.
- Publisher and source URL: Kenney; [official asset page](https://kenney.nl/assets/nature-kit); [exact official ZIP](https://kenney.nl/media/pages/assets/nature-kit/37ac38a37b-1677698939/kenney_nature-kit.zip).
- Checksum or integrity evidence: downloaded archive is `10,537,521` bytes with SHA-256 `fa7974a0d342bfe63c38664ba9f8ec1a4aab8ea25f099bdc56870e33588c4d9d`; `unzip -tq` passed. Kenney publishes no separate checksum on the asset page, so this is a locally recorded acquisition hash rather than a publisher checksum.
- Exact acquisition method: inspect the official asset page and direct link, confirm an HTTP `200` `application/zip` response and advertised size, download the exact HTTPS ZIP into project-local third-party staging, hash and integrity-test it, validate all archive paths, then selectively extract only the upstream license and 15 named GLB files. No downloaded code was executed.
- Purpose: supply premade low-poly palms, broadleaf/conifer/wind-shaped trees, shrubs, and grasses for a Treasure Island vegetation palette without drawing replacement plant meshes.
- Scope and locations: full archive and upstream license under `third_party_staging/kenney_nature_kit/` (excluded from export); 15 selected GLB assets totaling `237,944` bytes under `game/resources/models/vegetation/kenney_nature_kit/`; no account, purchase, software install, plug-in, or system-wide change.
- Authorizing decision or request: owner's 2026-08-28 explicit request to find and safely stage one reputable free premade vegetation pack when the official direct source was clear.
- License: Creative Commons Zero (`CC0`), as shown by both the current official page and the archive's preserved `License.txt`; attribution is optional.
- Trust and malware checks: official first-party HTTPS host; direct ZIP response; no compressed-data error; all `3,626` paths passed absolute-path, parent-traversal, and backslash checks; extension inventory contains only model, material, image, license, URL-shortcut, and directory entries; no executable or script format is present or extracted.
- Verification and result: completed. The selected files parse as glTF 2.0, contain `15` assets totaling `8,832` source vertices and `2,893` triangles, use embedded flat-color materials with no texture references, and cover the requested palette. Detailed archive and selected-file inventories are recorded in `third_party_staging/kenney_nature_kit/PROVENANCE.md` and `game/resources/models/vegetation/PROVENANCE.md`.
- Removal steps: after removing any project references, remove only `game/resources/models/vegetation/kenney_nature_kit/` and `third_party_staging/kenney_nature_kit/`; update this entry to `removed`.

### 2026-08-28 — USGS/USDA NAIP 2022 Treasure Island review crop

- Date and time (UTC): acquired `2026-08-28T23:15:55Z`; integrity/decoder checks completed in the same work session.
- Status: `completed`; review-only source in export-excluded staging, not runtime input.
- Exact data: one `2370x3181` natural-color TIFF crop of the frozen Treasure Island bbox `[-122.3790725,37.8151916,-122.3629075,37.8323315]`, sourced from adjacent locked ImageServer rasters `OBJECTID=21720` / `m_3712214_nw_10_060_20220518` and `OBJECTID=21715` / `m_3712213_ne_10_060_20220518`; both acquired `2022-05-18`, USDA / USDA-FSA-APFO, 0.6 m, four-band source.
- Publisher and source URL: USGS The National Map `USGSNAIPPlus` ImageServer, <https://imagery.nationalmap.gov/arcgis/rest/services/USGSNAIPPlus/ImageServer/exportImage>.
- Integrity evidence: TIFF `23,351,192` bytes, SHA-256 `0d69c3cad642539581afffbfbb332c58ee1a7bd046b0f434a13545ec7bbe479d`; ImageMagick identified exactly `2370x3181`, sRGB, 8-bit, three-channel RGB after the service's documented `NaturalColor` rendering function; Apple `sips` independently decoded the same dimensions.
- Exact acquisition method: account-free HTTPS `exportImage` GET with EPSG:4326 bbox/image CRS, `2370x3181`, U8 TIFF, bilinear interpolation, `NaturalColor`, and `esriMosaicLockRaster` IDs `[21715,21720]`. Exact URL and catalog query are retained in `third_party_staging/naip_2022_treasure_island/PROVENANCE.md`.
- Purpose: manually identify a conservative, reviewable candidate set of mature individual tree crowns without using or biasing toward the current generated vegetation placements.
- Scope and location: `third_party_staging/naip_2022_treasure_island/`, already excluded by `third_party_staging/*`; no account, credential, software, plug-in, executable, or system-wide change.
- Authorizing request: owner's explicit 2026-08-28 direction to continue with 2022 NAIP as the tree-position reference.
- License/attribution: USGS labels NAIP public domain; credit retained as `Imagery: USDA Farm Service Agency National Agriculture Imagery Program (NAIP), acquired 2022-05-18; served by USGS The National Map.`
- Verification and result: completed. The catalog lock prevents the Plus service from silently selecting HRO or another vintage. The bbox crosses two same-date adjacent records because the named point record ends about 110 m east of Treasure Island's western shoreline; using both preserves full island review coverage while still producing one crop.
- Removal steps: remove only `third_party_staging/naip_2022_treasure_island/` if this candidate annotation effort is intentionally abandoned; no runtime regeneration is required because staged imagery is not integrated.

### 2026-08-29 — Poly Haven six-material texture micro-bundle

- Date and time (UTC): acquisition planned at `2026-08-29T04:53:50Z`; completed at `2026-08-29T04:55:27Z`.
- Status: `completed`.
- Exact assets: Poly Haven `clean_asphalt`, `concrete_pavement`, `concrete_floor_03`, `sparse_grass`, `plaster_grey_04`, and `bitumen`; for each asset, only the official 1K JPG diffuse, OpenGL-normal, and roughness maps.
- Publisher and source: Poly Haven official asset pages and `https://api.polyhaven.com/files/<asset_id>` records; [CC0 license](https://polyhaven.com/license).
- Exact acquisition method: query the six official API records, resolve only the selected official HTTPS JPG URLs, download the 18 image files into project-local texture resources, then record exact URLs, byte sizes, MIME/decoder results, and SHA-256 hashes. No archive, installer, plug-in, script, executable, account, or credential is involved.
- Purpose: coherent world-scale asphalt, pedestrian concrete, paved-area concrete, sparse ground, exterior plaster, and flat-roof bitumen materials for the approved first playable.
- Scope and intended location: `game/resources/textures/world/polyhaven/`; no software installation or system-wide effect.
- Authorizing decision or request: owner-approved NEED-006 in [`discovery/RESOURCE_REQUESTS.md`](discovery/RESOURCE_REQUESTS.md), based on the owner's explicit 2026-08-28 direction to use textures and find a pack for the art-direction targets.
- Trust and stop conditions: official Poly Haven HTTPS/API hosts only; JPG images only; stop on an unexpected host, MIME type, file extension, decoder failure, or unexplained size/hash mismatch.
- Verification and result: completed. The 18 selected files total exactly `13,990,506` bytes. Every download came from the expected `dl.polyhaven.org/file/ph-assets/Textures/jpg/1k/` path, matched the exact byte size and MD5 in its official API record, and has a locally recorded SHA-256. `file` decoded every asset as an 8-bit `1024x1024` JPEG; all selected diffuse/normal maps are three-channel and roughness maps are valid one- or three-channel JPEGs. The complete URL pattern, per-file size, official MD5, local SHA-256, asset-page mapping, and CC0 provenance are retained in [`game/resources/textures/world/polyhaven/PROVENANCE.md`](game/resources/textures/world/polyhaven/PROVENANCE.md).
- Removal steps: after removing material references, remove only `game/resources/textures/world/polyhaven/`; update this entry to `removed`.
