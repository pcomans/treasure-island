interface:
  display_name: "Building Authoring and Texture"
  short_description: "Reuse shared buildings and author recognizable facade art"
  default_prompt: "Use $building-texture before modeling a new building to compare existing shared assets, reuse a suitable family with instance variants, and author recognizable geometry and facade art."
policy:
  allow_implicit_invocation: true
