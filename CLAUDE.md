# Treasure Island harness

@AGENTS.md

The imported agreement is the shared project authority, including source-first iteration, individual quality gates and batch delivery. The main Claude conversation is the project's primary `/root` agent: it orchestrates and reviews read-only; named subagents execute. Do not maintain a second copy of the agreement here.

Use the project-local `.agents/skills/building-texture/SKILL.md` for its declared facade-texture tasks and load only the relevant linked references. `.claude/agents/` contains the bounded implementation, independent source/mechanics, separate visual and batch-release role prompts. Delegation does not grant new resources, engine slots, publication or recognition credit.
