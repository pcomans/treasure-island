# HOLD — external comparison class registration; geometry unperformed

Independent Astra Medium diagnosis. Actual focused log eef9d7807ec1619eafe45a3f3257b5b290705788a251ab5265fd119966bbad41 reports `Class "WorldChunkBuilder" hides a global script class` at comparison/current-chunk-builder.gd:1. That external file begins `class_name WorldChunkBuilder`; focused_live_contract.gd:9 preloads it. Dependency compilation fails before the focused script runs. No focused result, source/live geometry checks or registry/loader observations exist. This is a preparation-helper failure, not evidence against either unit's geometry.

Actual import PID58417 exited0,73classes/fourUIDs; the material specular/glossiness conversion warning remains retained. Cache hash matches import receipt, canonical WorldChunkBuilder maps to res://game/scripts/world/world_chunk_builder.gd, and all four generated UID hashes still match. The conflicting external comparison was not required to be discovered by project import; explicit preload exposes its duplicate global declaration afterward. Focused PID58558 was terminated by the existing exact-entry fatal-load handler with exit−15; chain exit1. Both PIDs were absent at release f6f10e7650d38c1779e000ba63cb227b1d3a6b09a7abb6f5849aa09a5e8c672d, which records no engine processes, no results and no retry. Both later native stages are unrun.

Smallest helper-only successor: preserve the failed directory unchanged; make a successor external current-chunk-builder comparison copy with only the first `class_name WorldChunkBuilder` declaration removed. Retain `extends RefCounted` and every following byte. Original comparison SHA793baf69aeaf24b43c280234d1c85422f54aab835f3d658658e9951f4d584dc6 equals the current MAIN builder. There are no other WorldChunkBuilder self-references in this comparison. Existing focused `OLD_BUILDER.new()` sites instantiate the explicitly preloaded Script, so no rename, global registration or constructor rewrite is needed.

Rebind only the successor focused preload/path, fresh output/stage/prerequisite/release paths and affected helper/pin/seal joins. Keep the original full comparison hash as provenance and bind the exact one-line derivation; do not falsely call the corrected helper byte-identical to the source. Preserve all source methods, native channel/material/shape/ownership comparisons and predicates. Production candidate bytes, original-source records, accepted first-five/authority limits and successful import/cache/fourUIDs remain unchanged. A reviewed successor should run focused first using that exact successful import, followed by the previously unrun native registry and loader on success, under separate ROOT GO. No engine or import replay is warranted for this helper-only correction.

LEARNINGS.md:81 already warns that external frozen comparison helpers require explicit-script construction rather than assumptions about global registration; line85 separates project discovery from export exclusions. This incident is the duplicate-class form of that same boundary. Readiness c19f471f… missed the declaration despite reviewing helper/source comparisons; its actual execution is now correctly HOLD. No broad cache purge, production rename, geometry change, weakened check or new parser framework is justified.

No engine, source edits, checkout, MAIN changes or reruns were performed during diagnosis. No full-unit/live/package acceptance.
