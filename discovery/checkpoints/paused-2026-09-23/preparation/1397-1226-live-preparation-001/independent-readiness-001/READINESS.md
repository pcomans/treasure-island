# READY — shared1397+1226 isolated live source and native chain

Independent Astra Medium change review, reusing accepted1397 readiness44b07e8a… and both first-five packet reviews. FILE_READYbe753aa19e1202593199bc3d1c060468dd6a1e3818904fd963c2b8b703808828; prospective pins26151e515fd26a7491beec1376666e82a36534e888c839ad4c909fb20b4ac793; SEALdc5c84ef3f93adfeef939a07f654a9fe70ec86b349ada01a7bb612e6d9d4020c. All seal entries,3970 staged files,16 selected files and20 external pin inputs match. Prospective3990 map joins all staged bytes to the one fresh candidate; candidate and execution artifacts do not exist. Old1397 target factory/config/adapter bytes remain exact.

1226 H config is byte-identical. Actual factory delta is only explicit source/tangent callback plumbing and live self path, preserving pale band material, geometry, protected scope and receiver behavior. New atomic adapter is the reviewed1397 implementation specialized to w96215685/x_-2__z_-3, mapped5,6,7,8,9,11,12,14 and exact protected complement. Dependency hashes, aliases/drift/partial/duplicate guards, split identity roots, nested source/detail collision ownership and cleanup remain. Joint builder contains the complete1226 pair/preflight/plan/rollback/consume lifecycle once; exact dispatch now ends1397_plan,1226_plan. No gameplay/source construction change or fallback.

All213 registry unit records equal current32 and metric stays32/213. Both exact adapter paths/full hashes are consumed by JS scanner, native registry fixture and production loader with the existing format substitutions/privacy/missing-resource checks. Current outputs registry9247d365… and contracts6e76908b… bind compiler/native consumers. Actual shared compiler, Node registry and Node loader receipts are exit0 on first invocations. Existing explicit positive compiler dispatch statements and hash expectations remain independent.

Approved mutation experiment is correctly limited to three negative mutation positions. Selection runs after positive validation, requires exactly one named build_chunk body bounded by the next function and exactly one anchored two-tab record dispatch statement. This independently resolves to one current statement; the compiler retains four explicit full-call markers. Existing cardinality/non-no-op/placeholder operations and both specific Navy Chapel/D2 order-error assertions remain. Successful actual Node receipt supports one pass, zero token repairs and zero repeat invocations; no new production acceptance rule or parser framework.

Focused helper differs from reviewed1397 helper only in shared paths, two-spec cardinality/scope/output marker. It retains195+208 channel/material equality, four collision subtrees per unit, actual ordinary attachment, atomic rollback/consume/cleanup, non-target records and current32 root comparisons. Historical same-chunk callback raw b2394b15… actually gives29 for1397's prior1241 chunk and126 for1226's prior1232 chunk; unchanged producer joins verify. New13/14 yields42/140. Combined WORLD735/1287/1302/183761/500/667 remains an expected measurement, not actual new native evidence.

Materializer and chain are byte-identical to reviewed1397 helpers; runner changes only shared GO variable. Exact detached clean fdfc100 base is checked before destination writes; all staged files are hash-checked, existing hydrated bytes reused without downloading, and entire destination map verified before MATERIALIZED. Import expects73 classes and four new factory/adapter UIDs. Each dependent stage binds actual cache/UIDs and predecessor receipts; exact source/GO, fresh output, terminal/error/PID checks and finally-release remain. No compiler/Node replay needed for byte copying.

After separate ROOT materialization authorization, cwd MAIN:
```sh
/usr/bin/env PATH="/Volumes/Macintosh_HD/Users/user302070/code/treasure-island/.tools/git-lfs/3.8.0/git-lfs-3.8.0:/usr/bin:/bin:/usr/sbin:/sbin" GIT_LFS_SKIP_SMUDGE=1 /usr/bin/git worktree add --detach /Volumes/Macintosh_HD/Users/user302070/code/treasure-island-1397-1226-live-candidate-v1 fdfc100704200e6570b654a4e6a4fccc9a1a61be
```
Then cwd this preparation directory:
```sh
/usr/bin/python3 -B materialize-source.py --root-authorized-isolated-materialization
```
After actual destination verification and separate exact ROOT engine GO, same cwd:
```sh
/usr/bin/env TI_1397_1226_ENGINE_GO=fdfc100704200e6570b654a4e6a4fccc9a1a61be /usr/bin/python3 -B run-reviewed-chain.py import-001 focused-001 native-registry-001 native-loader-001
```
Release: SLOT_RELEASE-import-001-focused-001-native-registry-001-native-loader-001.json. No old1397 candidate/materialization or one-unit export required.

Both units remain unaccepted: source captures915/31 differ from prospectivefdfc100/32.1226 original rawHOLD, derived925 rows and actual later3sprays stay distinct;1397 retains finite663 rows/3callbacks. Hidden/protected faces, roof/jetpack unperformed limits and separate visual evidence remain. Actual live native results, package/role6 and seventh acceptance remain pending. No engine, source edits, copy, checkout, MAIN mutation or unchanged tests executed by reviewer.
