# Bounded preparation handoff; no execution authorization

1. Consume corrected candidate002 and actual independent package PASS plus real per-unit6/7 receipts and ROOT acceptance. Freeze real bindings in existing INPUTS; preserve historical31 first-five and candidate32 metrics.
2. Resume the retained1240 prepare-authority.py as a two-unit writer using before/, unit-expectations.json, units/*/authority-record.proposed.json and consumer-adaptation.proposed.json. Donor recipes are frozen with .historical suffixes. They are not runnable successor scripts. Final receipt-dependent authority generation stops here honestly: actual seventh paths/hashes do not exist. Do not invent them or run the historical writer.
3. After authorized isolated source preparation and complete selected whitespace checking, existing commands are `/opt/homebrew/bin/node tools/build_facade_recognition_registry.mjs --write`, adapted existing bind-compiled.py using actual compiler outputs, `/opt/homebrew/bin/node tools/build_facade_recognition_registry.mjs --check`, `/opt/homebrew/bin/node tools/test_facade_recognition_registry.mjs`, and `/opt/homebrew/bin/node tools/test_facade_runtime_loader_contract.mjs`. Exact isolated cwd and final selected files require the next ROOT assignment. None ran here.
4. Co-adapt frozen runtime runner/cache/prerequisite/release and all scanner consumers before independent readiness. Retain future `/usr/bin/python3 -B run-source-chain.py import-001 headless-registry-001 headless-loader-001`; bind actual absolute paths/maps/compiler/cache inputs before serialized ROOT engine GO. Do not replay completed applicable checks merely for logging.
5. After independent registry acceptance, adapt current-package donor with the corrected shared002 batch-neutral binder. One exact-current34 source-component/export/mounted-component/mounted-privacy/headless/native chain must verify both real attachments and all invoked scalars/WORLD. Bind actual app/PCK only after export. Candidate PCK remains historical32.
6. Merge documentation-staged (including accepted audit002 RETRO) into ordinary publication with actual final release facts. Adapt existing publication writer/selected path list and include exact new packet trees. Preserve B1/HUMAN and prior32; perform independent release/privacy/publication review, complete selected and final staged whitespace checks. Materialization/commit/push only after actual acceptances and ROOT GO.

No new framework, gate, authority, MAIN edit, credit, engine/test/export or Git action was introduced. Static proposal is useful preparation, not FILE_READY for execution.
