# Treasure Island Administration Building 1 exact-run ownership handoff

Date: **2026-08-30**  
Target: **Treasure Island Administration Building 1 / Treasure Island Museum**  
Source / receiver: **`r16681702` / `building:r16681702:wall`**  
Stage: **mapping prerequisite complete; partial detached calibration eligibility only**

## Outcome

The approved local observations can be reconciled to exact generated public-outer receiver regions without styling or placing anything. They do **not** support a complete seven-family placement study.

- A later detached calibration may test `B1-MAT-IVORY` on a narrowed exact public-outer field scope. Runs `21..51` are the preferred smallest contiguous scope because they form the unambiguous `85.940 m` broad public curve.
- `B1-CENTRAL-W` may enter a later detached study only as a sparse stylized exemplar somewhere inside the exact receiver region `21..51`; this mapping does not choose an anchor.
- `B1-PAV-W` may enter a later detached study only as a sparse stylized exemplar within the exact north-end or south-end candidate regions listed below; this mapping does not choose an anchor or authorize one at each end.
- `B1-WING-W`, `B1-BASE-O`, `B1-DOOR-BLUE`, and `B1-BAND` remain placement-blocked because the retained observation notes do not tie those features to an exact frozen endpoint/run subset.

No run assignment below is a claim of a real opening coordinate, count, cadence, sequence, story stack, or completed elevation.

## Facts, observations, and inferences

### Verified geometry facts

- The generated wall remains `110` ordered quads / `414.381 m`, `20 m` high, at flat base/top `6.281 / 26.281 m`.
- Runs `0..96` are one closed main outer component: `97` runs / `378.769 m`, centroid `[-3.935, 732.070]` in world `[X,Z]` metres.
- Runs `97..99` are a separate closed generated outer sliver: `3` runs / `10.352 m`, signed plan area `0.0561 m²`, centroid `[5.616, 746.770]`. Its exact generated ownership is verified; a likely relation-union cause is not used as appearance evidence. It is too small and semantically unrelated to host a facade module.
- Runs `100..109` are the closed inner ring: `10` runs / `25.260 m`. Each endpoint pair exactly reverses one run of the separately generated `building:w1222720021:wall` (`100→9`, `101→8`, …, `109→0`). They are excluded, not merely low-confidence.
- The source relation retains outer members `w1169707889` and `w34313541` and inner member `w1222720021`. The tower is a separate logical/physical receiver.

### Approved reference observations

The only appearance evidence is the existing `B1-SV01..03` textual ledger. Their projected plan positions are:

| Observation | World `[X,Z]` m | Heading / FOV | Recorded visible role |
|---|---:|---:|---|
| `B1-SV01` | `[-88.714, 761.876]` | `55° / 75°` | wide public curve, wings in context, central tall-window rhythm |
| `B1-SV02` | `[-92.073, 694.472]` | `105° / 75°` | north end/return, wing windows, base openings, utility door |
| `B1-SV03` | `[-63.530, 806.482]` | `55° / 75°` | south end pavilion and broad public curve |

The diagnostic samples seven points per run and requires the observer to be on the outward side, the point to lie inside the recorded horizontal FOV, and no other outer run to occlude the point. This is a plan-visibility screen, not pixel analysis. Appearance and semantic-family statements continue to come only from the approved observation ledger.

### Bounded production inferences

- `PUBLIC-CURVE-CONFIRMED` is an unambiguous plan match because the continuous curved chain `21..51` is directly visible across the WSW/NW/SW observations and all three ledgers name that public curve.
- The north and south end **regions** can be bounded from observer side, endpoint continuity, and plan shape. The observations do not identify a real motif-bearing run within either region.
- The warm-ivory field can be considered over directly observed public faces because the approved ledger records it as the dominant field across the public elevation. Exact metre scale and phase remain a later reversible calibration choice.

## Exact run-region partition

| Mapping region | Ordered runs | Length | Status / reason |
|---|---|---:|---|
| `PUBLIC-NORTH-END-CANDIDATE` | `93..96, 0..8` | `58.505 m` | Medium-confidence public north/end mapping from `B1-SV02`; individual motifs unresolved. |
| `PUBLIC-NORTH-LINK-CANDIDATE` | `9..20` | `35.452 m` | Directly visible in southern/WSW views; family transitions unresolved. |
| `PUBLIC-CURVE-CONFIRMED` | `21..51` | `85.940 m` | High-confidence exact public-curve region; internal tall-window/wing boundary unresolved. |
| `PUBLIC-SOUTH-END-OBSERVED-FACES` | `52..56, 58, 60` | `27.012 m` | Medium-high-confidence south-end public faces from `B1-SV03`; individual pavilion group run unresolved. |
| `SOUTH-END-OCCLUDED-RETURNS` | `57, 59, 61` | `8.087 m` | Protected: approved observers are not on these SE-facing return sides. |
| `REAR-EAST-UNOBSERVED` | `62..92` | `163.773 m` | Protected: explicitly unobserved east/rear outer chain. |
| `SECONDARY-OUTER-UNION-SLIVER` | `97..99` | `10.352 m` | Verified generated component, facade-ineligible. |
| `INNER-RING-TOWER-HOLE` | `100..109` | `25.260 m` | Verified excluded inner ring; reverses separate tower receiver. |

These eight groups partition all `110` runs exactly once.

## Asset-family eligibility

“Eligible receiver region” means only that a later detached calibration may choose a sparse, explicitly stylized exemplar inside it. It does not claim the real building has the motif at every listed run, and this handoff authorizes no anchor.

| Asset | Exact eligible / candidate scope | Ineligible or protected scope | Gate |
|---|---|---|---|
| `B1-MAT-IVORY` | Exact field-eligible public faces `0..56, 58, 60, 93..96` (`63` runs / `206.909 m`); preferred bounded study `21..51` (`85.940 m`) | `57,59,61` unresolved returns; `62..109` ineligible/protected | **partial calibration authorized**; field only, no scale accepted |
| `B1-WING-W` | Unresolved candidate envelope `9..51` (`43` runs / `121.391 m`) | Every run outside that candidate envelope | **blocked**: no recorded wing/central transition endpoint |
| `B1-CENTRAL-W` | Exact receiver region `21..51` (`31` runs / `85.940 m`); no anchor | All other runs | **partial calibration authorized** for a sparse stylized exemplar only |
| `B1-PAV-W` | North/end region `93..96,0..8` plus observed south faces `52..56,58,60` (`20` runs / `85.517 m`); no anchor | All other runs, including occluded south returns | **partial calibration authorized** for a sparse stylized exemplar inside a separately justified end region only |
| `B1-BASE-O` | Unresolved north-view candidate `93..96,0..20` (`25` runs / `93.957 m`) | Every run outside that candidate envelope | **blocked**: base feature not tied to north end vs return vs link; window/vent remains unresolved |
| `B1-DOOR-BLUE` | Unresolved north-view candidate `93..96,0..20` (`25` runs / `93.957 m`) | Every run outside that candidate envelope | **blocked**: utility door not tied to an endpoint-relative run |
| `B1-BAND` | Unresolved parking/public candidate `9..56,58,60` (`50` runs / `148.404 m`) | Every run outside that candidate envelope | **blocked**: no run or terminal is recorded for the muted rose accent |

The machine-readable registry contains every exact endpoint, midpoint, length, tangent, outward normal, facing, component owner, region, and per-observation visibility count: `TREASURE_ISLAND_BUILDING_1_RUN_OWNERSHIP.json`.

## Exclusions and stop conditions

- No asset has been placed, calibrated, attached, integrated, resized, or reinterpreted.
- Runs `97..99`, runs `100..109`, and the separate tower `w1222720021` are categorically excluded.
- East/rear runs `62..92` and south return runs `57,59,61` cannot inherit public-face appearance.
- Main entrance/canopy, emblems, exact module dimensions/coordinates, counts, cadence, sequence, exact family transitions, completed elevation, and whole-building resemblance remain blocked.
- The four blocked module families may not enter calibration from the current notes. New exact feature-to-plan evidence or an independently authorized narrower interpretation would be required; orientation buckets or visual convenience are not sufficient.

## Reproducible evidence and checks

Evidence directory: `evidence/first-playable/treasure-island-building-1-run-ownership-mapping-2026-08-30/`

- `plan-view.svg` / `plan-view.png`: exact run order, regions, ring/tower exclusion, and observer headings.
- `visibility-diagnostic.json`: seven-sample per-run visibility results and observation projections.
- `README.md`: reproduction, interpretation boundary, and inspection findings.
- `checksums.sha256`: sealed mapping evidence and supporting contract identities.

Focused checks:

- `node tools/build_building_1_run_ownership_mapping.mjs`: **PASS**, generated output byte-for-byte deterministic.
- `node tools/test_building_1_run_ownership_mapping.mjs`: **PASS**, `110/110` partition, exact chunk endpoint equality, `97/3/10` component split, reverse tower match, partial `3`-asset gate, four exact blockers, zero placements.
- Original Building 1 standalone evidence checksum: **19/19 OK**.
- Runtime/generated source pins remain unchanged; no Godot/native capture was needed because this is plan ownership, not an art or placement review.

## Next bounded round

A detached calibration round is **authorized only in part**: narrow `B1-MAT-IVORY` to an exact defensible public scope, then consider sparse `B1-CENTRAL-W` and/or `B1-PAV-W` exemplars inside their mapped receiver regions with production-inference anchors and all survey/count/cadence flags false. It must omit `B1-WING-W`, `B1-BASE-O`, `B1-DOOR-BLUE`, and `B1-BAND` unless their exact mapping blockers are separately resolved. Independent exact-receiver art review remains required before any live integration.
