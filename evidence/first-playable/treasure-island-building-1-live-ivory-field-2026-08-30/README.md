# Building 1 live ivory field evidence

Status: `pending_independent_actual_world_art_review_not_self_accepted`

This native Godot 4.7.2 Forward+/Metal package shows only the independently placement-authorized `B1-MAT-IVORY` field on the actual loaded receiver `building:r16681702:wall`. Exact ownership is outer-ring runs `21..51`: 31 ordered runs, 85.939934 m source chain length, and 1740.731069 m² source surface-area semantics. No Building 1 module is live.

## Exact live result

- One shared-miter overlay mesh, one surface, 124 vertices, and 62 triangles.
- Runtime delta: `924→925` meshes, `934→935` surfaces, `50,729→50,791` triangles, and `466→466` collider pairs.
- All 30 adjacent rendered endpoint pairs remain watertight at `0.0 m`; cumulative chain-metre UV phase remains continuous with `0.0 m` join delta.
- The receiver mesh, receiver collider, navigation, spray ownership, generated data, and other receivers remain unchanged.
- Live module count is exactly zero. CENTRAL and PAV remain detached fit studies only; WING, BASE, DOOR, and BAND are not attached.

## Evidence

Eleven native 1440×900 PNGs cover the review-identified `38→39` join, the maximum-miter `42→43` join, scope-start `21→22`, the full 85.939934 m public curve, nearest and ordinary gameplay distance, oblique and multi-side context, whole-building context, changed light, and the whole island. `capture-manifest.json` records each camera, renderer, image checksum, exact live metadata, and truth boundary. `runtime-isolation.json` records the authoritative before/after topology and zero collision/navigation/spray/module delta. `visual-verification.json` records executor validity inspection without an art verdict. `checksums.sha256` seals the package.

## Review boundary

Independent actual-world review may judge only the exact ivory field on outer runs `21..51`. Surveyed material scale, other runs/sides, inner ring `100..109`, separate tower `w1222720021`, actual module anchors/count/cadence/sequence, complete elevation, and whole-building resemblance remain blocked. This package does not self-accept or promote the pending lifecycle scope.
