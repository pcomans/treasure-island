# Building 1 standalone facade prototype evidence

Status: **ready for independent standalone art review; not self-accepted**  
Target: Treasure Island Administration Building 1 / Treasure Island Museum  
Source / receiver: `r16681702` / `building:r16681702:wall`

This is a controlled standalone proof for one warm-ivory homogeneous material field and six complete shallow module families: `B1-WING-W`, `B1-CENTRAL-W`, `B1-PAV-W`, `B1-BASE-O`, `B1-DOOR-BLUE`, and `B1-BAND`. It is not exact-receiver calibration, a placement proposal, a complete elevation, or a whole-building comparison.

## Native run

Captured once, without an art or framing correction pass, using trusted Godot `4.7.2-stable (official)`, native macOS display, Forward+, Metal, and `Apple M1 Pro (Apple7)` at `1440x900`:

```text
.tools/godot/4.7.2/Godot.app/Contents/MacOS/Godot --path . --display-driver macos --rendering-method forward_plus --rendering-driver metal --audio-driver Dummy --resolution 1440x900 --script game/tests/building_1_standalone_prototype_capture.gd
```

The harness reported `PASS` after writing eleven images and `capture-manifest.json`. `checksums.sha256` (`d1e7ab8987f64d01165555fdade3b52dfa042b1ef1a9d5eb2b32ec88cb85ac65`) validates all `19` listed images, evidence records, registry, factory, scene, capture harness, and headless contract.

## What the images prove

- `01` is the neutral material close view. The field is intentionally restrained; the low band in frame is the separate `B1-BAND` exemplar, not material content.
- `02` exposes the full `40 m` field span for macro-repeat and distance behavior.
- `03..08` name and show every complete module family. Some close views retain neighboring exemplar edges; the named target remains complete and readable.
- `09` shows shallow relief and all six families obliquely.
- `10` shows ordinary-distance readability.
- `11` changes the sun direction and exposure to expose matte response, opaque proxy behavior, and module depth.

The panel is a proof stand only. The six modules are intentionally separated exemplars and must not be read as real order, count, cadence, or placement. Each module owns zero warm-ivory field/backing meshes; the single field proof is a separate host mesh.

## Deterministic contract

| Scope | Meshes | Surfaces | Triangles |
|---|---:|---:|---:|
| Field proof | 1 | 1 | 12 |
| `B1-WING-W` | 7 | 7 | 84 |
| `B1-CENTRAL-W` | 9 | 9 | 108 |
| `B1-PAV-W` | 13 | 13 | 156 |
| `B1-BASE-O` | 6 | 6 | 72 |
| `B1-DOOR-BLUE` | 5 | 5 | 60 |
| `B1-BAND` | 3 | 3 | 36 |
| **Total** | **44** | **44** | **528** |

Collision, navigation, spray and module-owned field/backing nodes are all zero. One local unit is one metre. All module dimensions and the `1.10 / 0.32 m` material scales are reversible production inferences, not survey results.

## Provenance and isolation

The appearance basis is limited to the project-owned `TREASURE_ISLAND_MUSEUM_BUILDING_1_REFERENCE_SPEC.md` and its `B1-SV01..03` observation ledger. No source pixel was stored or copied and no external resource or generative service was used.

`runtime-isolation.json` pins the unchanged generated manifest/chunk, world builder, accepted-material helper and canonical receiver inventory. It also pins every interrupted, unreviewed `r133351` byte exactly as found; this round did not modify or review that work.

## Claims still blocked

- Exact outer-run endpoints, ordered runs, anchors, coordinates, counts, cadence, or sequence.
- Main entrance/canopy, pavilion emblems, east/rear facade, inner-ring runs `100..109`, or separate tower `w1222720021`.
- Surveyed dimensions, scale, color, finish, transparency/interiors, or glazing subdivisions.
- Receiver massing/silhouette correction, exact-receiver acceptance, complete elevation, or whole-building resemblance.

Independent review must keep the material-field verdict, each complete-module verdict, placement/elevation verdict, and whole-building verdict separate.
