# 1308 live candidate evidence

Independent same-app candidate package PASS is bound to this unit. Authority remains **11/213**, this unit gains **zero credit**, and promotion is false. The final independent unit packet attestation remains pending. All prior units and the 213-unit inventory are unchanged.

The 14 selected native images are byte-identical copies. 1308 uses three corrected-trim stills and nine sampled payloads, plus historical pre-trim spray/changed-light context stills for unchanged features only. Its 06 camera forward Y differs by 1.9371509552e-7; position/light are exact and orientation is tolerance-level. Earlier occluded whole views are not counted as usable facade evidence. 1394 uses five live stills and seventeen original sampled payloads, including a labeled startup HUD frame. Each unit's own manifests and reviews define its scope; the other unit's evidence does not supply its gate.

These are isolated live-world source captures, not combined-world or app screenshots. The exact unit factories/adapters/configs/materials were reused. The combined builder was checked against both units, then a smoke-only main delta measured both actual pairs in the signed app at 735 rows / 993 meshes / 1008 surfaces / 76376 triangles / 466 bodies / 472 shapes. Historical isolated totals remain unchanged in their raw records.

All derived summaries explicitly preserve raw hashes and normalization lineage. The shared ignored private sibling retains owner/reviewer programs, source revisions, bindings, logs, original failures, native originals, all three original movies and every native image payload (160+163+161). Each movie retains its 70-byte RIFF trailing-index discrepancy. The historical 1308 blue shutdown frames and newer held endpoint/blue teardown are preserved. Temporal judgment is sampled; no continuous playback, general-player validity or AVI/physics counter equivalence is claimed.

Original strict contact failures and outgoing support-identity limits stay distinct from accepted scoped traversal. Owner focused001 parse and mounted001 reporting errors remain FAIL; the successful focused002 keeps its historical output filename. Only detached-node diagnostic printing changed before mounted002. The independent plistlib setup failure is retained; native plutil supplied the accepted bundle check. Editor target-record dispatch supplies component evidence, while the supported signed executable supplies whole-main attachment. No M1/manual-play claim follows.

Public summaries contain normalized string-leaf paths; raw private originals retain original paths. Both unit packets share the same private source/package retention and exact app. The image-tree digest excludes its own document and differs from that document's hash. Existing reference packets and prior reference evidence remain in place; no reference photos were added to this packet or app. A separate independent attestation must bind the six artifact values and exact image-tree digest. No placeholder is an acceptance record.
