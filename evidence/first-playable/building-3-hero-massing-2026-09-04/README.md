# Building 3 arched-massing repair — pending independent review

This packet is the builder's exact-current technical handoff for Building 3
(`w34313540`). It is **not** an art or recognition verdict.

The prior flat-runtime frames remain immutable in
`../p1-existing-live-revalidation-2026-09-04/images/building-3/`. The two new
native Metal frames are paired to those prior whole/oblique roles in
`capture-manifest.json`. Both new frames use the real stock player rig after a
physics settle, retain the normal reticle and full world context, first-hit the
exact wall receiver, and contain the complete observable wall/roof mass.

## What changed

- The exact frozen wall and roof X/Z geometry remains the horizontal authority.
- The flat generic wall/roof runtime is intercepted before construction and is
  replaced by one shared arched profile with four tapered corner shoulders.
- Visible wall and collision use `236` triangles; visible roof and landing
  collision use `675`. This is `+766` triangles over the former `118 + 27`,
  with zero mesh, surface, body, or shape-count delta.
- The ENE facade follows the same wall profile, preserves its single large
  hangar opening, adds shallow tapered pylon-face relief, and exposes the upper
  band through a stepped pale projection with a narrow neutral inferred
  occlusion groove. The groove communicates depth in grayscale; it is not an
  observed-paint claim.
- Correct identity is NPS NRHP `08000083`. Frozen OSM `08000081` remains only
  as explicitly incorrect source provenance.

All vertical dimensions, the exact pylon influence, the pylon-face relief, and
the neutral depth groove are reversible production inference. The work is
exterior-only; it makes no surveyed, interior, construction-detail, or as-built
claim and ships no source photography.

An independent reviewer must open all four before/after PNGs at original detail
and apply the frozen confusion set (`w24274434`, `r16681702`) and whole-entity
top-1 rubric. The builder did not self-accept this packet.
