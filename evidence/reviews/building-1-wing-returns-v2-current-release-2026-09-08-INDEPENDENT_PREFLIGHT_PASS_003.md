# Current B1 release wrappers — independent preflight revision 003

Reviewer: `handoff_state_review` · 2026-09-08

PASS_FOR_B1_RETURNS_V2_CURRENT_RELEASE_CAPTURE_PREFLIGHT

The canonical owned-launch guard and explicit metadata type guard pass the
independent affected controls. This is a preflight verdict for the planned
source-render capture stage; no rendered evidence or release visual acceptance
is produced here. Both earlier HOLD notes and their rejected revisions remain
unchanged. Actual capture remains subject to root's coordinated stage release.
Recognition remains **9/213, +0**.

## Exact sealed inputs

Task directory: `work/b1-v2-release-capture-001`.

- `current-release-binding.json`: 25,208 bytes,
  SHA-256 `a9fb95a17c1cbe3b7d9e1d29465a5df3f08ac364d83b80aa58f8f0ea09993cdc`.
- `building_1_wing_returns_v2_current_release_capture.gd`: 8,238 bytes,
  SHA-256 `e93d3899ff05f1ce8f15b0bfb79a96fab06cab97b81a4073c30cc291d205c632`.
- `headless_building_1_wing_returns_v2_current_release_evidence_contract.gd`:
  15,833 bytes, SHA-256 `fb1f3b946367565ebaa16866b204d6c5247add17ca7029fed329f520cecc6c15`.
- `preflight-behavior-diff-003.json`, SHA-256
  `cfc5265d01e8e9a83b757120153c0deebbaf1af9e67cfe0900fb7fcd29bf199f`.

## Scope and observed checks

Compared directly with retained revision 002, only shared
`_release_command_is_fixed60` changes; the evidence wrapper also adds matching
`typeof` before raw metadata equality. No methods were added or removed. Eight
shared helpers have identical executable text across wrappers. The binding
changes only its existing guard explanation: all 51 audited files, three parent
programs, two reviews, seven app files, two complete package receipts and 49
accepted-reference pins retain their already-verified values.

The guard matches the observed owned PID's command to one exact canonical form.
The exact executable is immediately followed by `--fixed-fps 60`, before every
pathname and user argument. The remaining sequence binds the actual project and
executing script paths and the actual user-argument vector. It accepts either
the explicit native capture driver/settings sequence or the headless probe
sequence. It does not search flattened text for flag-shaped words. Different
argument orders or extra engine flags intentionally reject; use the canonical
form when the capture stage is released.

Independent scratch subclasses disable inherited capture initialization. The
capture subclass is the actual executing script, so its script identity and
owned-process context are real. All seven jobs use the exact absolute editor
executable; each process log is clean and terminal exit 0:

| Actual launch | Result | PID |
| --- | --- | ---: |
| `positive` | canonical fixed60; 23 controls pass | 2649 |
| `missing` | absent flag rejects | 2666 |
| `wrong30` | 30 FPS rejects | 2677 |
| `duplicate60` | duplicate engine flags reject | 2680 |
| `user-only` | user-only marker rejects | 2685 |
| `marker-in-script-path` | marker in one spaced pathname rejects | 2690 |
| `positive-user-delimiter` | engine 60 plus user-side 30 marker; 23 controls pass | 2698 |

The positive profiles check canonical headless and native command forms without
rendering; actual script paths contain spaces, and the parser also accepts an
exact executable path containing spaces. Missing/wrong/duplicate/multiline and
unobserved suffix controls reject. Eleven wrong-typed raw metadata fields and a
missing metadata field reject explicitly, without interpreter diagnostics.
Valid current metadata still satisfies inherited observation checks, and the
projection leaves both current and accepted dictionaries unchanged.

Unchanged geometry/camera/receiver/viewport rejection findings from revision 002
remain applicable: the new condition only rejects type mismatches before the
same equality/projection code. Render, controller, lighting, grayscale, spray,
landing, still/motion pairing methods and tolerances are unchanged; their earlier
independent mechanics evidence is reused. This round does not repeat source
routes, package smoke, captured-image checks or any visual verdict.

The planned scope remains 46 fresh PNGs plus three manifests, compared against
49 immutable accepted candidate reference files. Raw current metadata is checked
before identity-only in-memory projection. No observed geometry, camera or
motion values are replaced; no accepted or current manifest file is rewritten.
The images will be source renders with the separate exact signed-package proof
already independently accepted. They are not claimed as package-rendered pixels.

## Reproducible receipt and release state

Task `work/b1-v2-release-preflight-independent-003/launch-receipts.json`
records every exact argv array, cwd, PID, terminal exit, elapsed time and full
raw-log hash; SHA-256 `e054e822cc960eb75b687d3a0729dde88ac7a12d052e760003779705ba8a78f9`.
`static-delta.json` in the same directory binds the narrow method/binding delta
and unchanged prior HOLD notes; SHA-256
`7de91d693a83747a8700973532fe7cfeb1569fd5a3a68584f0ee4722e9e567f9`.
`owned launch probes/preflight-probe.gd` contains the bounded disabled-render
controls; SHA-256 `5c4e63748b28f2c01b9978a2d4a5c5d0500f956d5cb6dfa1ce87bdbc97c95c45`.

Coordinator session **67909** is terminal exit 0; the last owned process,
PID **2698**, exited 0. The heavy slot is released. Only this review, its RETRO
entry and scratch diagnostics were written; sealed wrappers, product, app,
authority and accepted evidence were not edited. Root may now assess release of
the planned capture stage, followed by its evidence and independent visual gates.
