# Held building drafts — 2026-09-24

These are inert source patches and actual game images for1397Gateview,1439Chinook and1444Croaker. Neither patch is applied. All three buildings remain held; this checkpoint grants no production, recognition or release acceptance.1444 is preserved separately as the held study and an unverified ceiling attempt.

The 1397 and 1439 patches are replayable against exact published base `6f44545c4c22eb722fe66abcf7039674ccd3c8b6`.1397 contains four selected files; its integration callback already exists in that base.1439 contains six files including its own builder callback change. The P2 builder's1444 delta is excluded. The 1397 and 1439 patches are separate and touch disjoint paths. Temporary-copy applicability/whitespace checks and resulting source hashes passed; no production checkout was modified.

1397 remains held for85.957mm retreat and4.040mm lower-join LAND penetrations, missing dependent retreat, and wall-spray footprint interference.1439 remains held for86.289mm wall-retreat penetration. Completed source, appearance, route, flight, query and callback scopes remain reusable as explicitly bounded in the manifest's independent reports. No successful-stage replay is proposed.

The six 1397/1439 PNGs are unaltered actual game originals:1397mechanics004 and1439productionactual002, labeled by building and whole/three-quarter/near view. They are held-draft illustrations, not acceptance evidence by themselves. Dated private real-world photos and full diagnostic movies stay outside Git. `manifest.json` binds selected source bytes, base dependencies, original images and required external evidence.

1444 lineage: `1444-study-held.patch` (A) applies to published `01845490ecab848400888126a84fa513740fda5d`, containing only1444 resources/adapter and its builder callback. `1444-ceiling-attempt-unverified.patch` (B) applies AFTER A; it preserves the HIGH attempt and its two1444 adapter-contract pin updates, excluding generated UIDs and unrelated JSON formatting. Never apply B directly to bareMAIN. Both layers were checked sequentially on temporary copies only.

1444 study has whole-building static PASS but close-ceiling visual HOLD and pending north-mouth evidence. Its three original actual-world001 PNGs are **STUDY ONLY**. The HIGH ceiling attempt has NO candidate images: two failed invocations,29 consumed pin checks passing, but unique consumed native physical union FAIL after validated engine snapping. The informative map is not READY. No art, mechanical or release acceptance is implied. Any future work requires a new owner resumption after quota pause; successful stages are not replay instructions.

The filtered attempt contract has no standalone HIGH hash literal, but the generic runtime loader checks it against `registry.adapter_contract.sha256`. That registry/loader binding remains stale. The inert patch is not a runnable or readiness claim; no registry/source repair is included.
